05-03 03:39:11 jdbc[3]: exception
org.h2.jdbc.JdbcSQLException: Syntax error in SQL statement "CREATE TABLE WEAPON(
  ID INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
  NAME VARCHAR(50) NOT NULL,
  WEAPON_QUALITY_ID INT NOT NULL,
  WEAPON_TYPE_ID INT,
  DESCRIPTION VARCHAR(500),
  FOREIGN KEY (WEAPON_QUALITY_ID) REFERENCES [[*]PTU].WEAPON_QUALITY(ID),
  FOREIGN KEY (WEAPON_TYPE_ID) REFERENCES [PTU].WEAPON_TYPE(ID)
) "; expected "identifier"; SQL statement:
CREATE TABLE WEAPON(
  ID INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
  NAME VARCHAR(50) NOT NULL,
  WEAPON_QUALITY_ID INT NOT NULL,
  WEAPON_TYPE_ID INT,
  DESCRIPTION VARCHAR(500),
  FOREIGN KEY (WEAPON_QUALITY_ID) REFERENCES [ptu].WEAPON_QUALITY(ID),
  FOREIGN KEY (WEAPON_TYPE_ID) REFERENCES [ptu].WEAPON_TYPE(ID)
) [42001-185]
05-04 16:54:48 jdbc[3]: exception
org.h2.jdbc.JdbcSQLException: Table "MOVE" not found; SQL statement:
ALTER TABLE MOVE (
  ID INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
  NAME VARCHAR(30) NOT NULL,
  FREQUENCY VARCHAR(30) NOT NULL,
  TYPE_ID INT NOT NULL,
  ACCUARACY_CHECK INT NOT NULL,
  DAMAGE_BASE_ID INT,
  MOVE_CLASS_ID INT NOT NULL,
  RANGE VARCHAR(50),
  EFFECT VARCHAR(1000) NOT NULL,
  CONTEST_TYPE_ID INT NOT NULL,
  CONTEST_EFFECT_ID INT NOT NULL,
  SPECIAL_ID INT,
  FOREIGN KEY (TYPE_ID) REFERENCES TYPE(ID),
  FOREIGN KEY (DAMAGE_BASE_ID) REFERENCES DAMAGE_BASE(ID),
  FOREIGN KEY (MOVE_CLASS_ID) REFERENCES MOVE_CLASS(ID),
  FOREIGN KEY (CONTEST_TYPE_ID) REFERENCES CONTEST_TYPE(ID),
  FOREIGN KEY (CONTEST_EFFECT_ID) REFERENCES CONTEST_EFFECT(ID),
  FOREIGN KEY (SPECIAL_ID) REFERENCES CAPABILITY(ID)
) [42102-185]
